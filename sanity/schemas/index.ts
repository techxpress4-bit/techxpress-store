import { categorySchema } from "./category";
import { productSchema } from "./product";
import { bestSellersSchema } from "./bestSellers";

export const schemas = [categorySchema, productSchema, bestSellersSchema];
